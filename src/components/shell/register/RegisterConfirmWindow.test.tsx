import * as React from 'react';

describe('RegisterConfirmWindow', () => {
  /*
  const renderRegisterConfirmWindow = () => {
    const props = {
      login: {
        email: 'test@milou.de',
        password: '',
        confirmPassword: '',
      },
      onFail: jest.fn(),
      registerConfirmed: jest.fn(),
    };
    return {
      component: render(<RegisterConfirmWindow {...props} />),
      ...props,
    };
  };
  */
  test('should confirm uuid', () => {});
});
